# Database integration

MongoDB Atlas and Mongoose are selected for this project. `mongoose.ts` provides the server-side reusable connection utility used by the development-only connection-status route. No medical-record schema is implemented.

# MongoDB Atlas connection string. Required only when database-backed features are implemented.
MONGODB_URI=

# Application URL for future authentication and link generation.
NEXT_PUBLIC_APP_URL=http://localhost:3000

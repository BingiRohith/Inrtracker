# Database

**Status: PLANNED**

MongoDB Atlas with Mongoose is selected. No connection code, collection, schema, index, or medical record is implemented yet. `MONGODB_URI` is documented in `.env.example` for the future connection layer.

Proposed entities for later review: User, Doctor, Patient, DoctorPatient relationship, INRRecord, DosagePlan, DosageSchedule, MedicalReport, Visit, ClinicalNote, and AuditLog. Their fields and relationships are deliberately not specified in Sprint 0.
